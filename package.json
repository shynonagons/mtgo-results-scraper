{
  "name": "mtgo-results-scraper",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "start": "node server",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.2",
    "jsonframe-cheerio": "^3.0.1",
    "request": "^2.88.0",
    "underscore": "^1.9.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
